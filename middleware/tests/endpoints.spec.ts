import { routes } from "../src/0_routes/routes";
import request from "supertest";

const api = routes;

/*describe("Products, The GET with /id", () => {
    it("GET should get a single product", async () => {
      const result = await request(api).get("/api/products/6");
      expect(result.body).toEqual({'no':79,'name':'flour','price':31});
      expect(result.status).toEqual(200);
    });
});*/

//endpoints
/*
  1. get all products
  2. get product by id
  3. post product
  4. update product
  5. delete product
  6. defualt route
*/

//roles testing
/*
  admin
  regular
  anonymous
 */




